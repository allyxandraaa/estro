# API base path used by src/api/client.js. Default keeps it same-origin
# so the Vite dev proxy (see vite.config.js) can forward to the backend.
VITE_API_BASE_URL=/api

# Backend origin the dev proxy forwards /api/* to. Only used in `vite dev`.
VITE_BACKEND_ORIGIN=http://localhost:3000
